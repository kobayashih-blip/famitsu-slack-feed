{
  "name": "famitsu-news-slack-feed",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
